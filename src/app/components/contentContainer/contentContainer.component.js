import controller from './contentContainer.controller';
import style from './contentContainer.scss';
import template from './contentContainer.html';

const contentContainer = {
  transclude: true,
  bindings: {},
  template,
  controller,
  style,
};

export default contentContainer;
