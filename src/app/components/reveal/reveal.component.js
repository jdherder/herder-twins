import controller from './reveal.controller';
import style from './reveal.scss';
import template from './reveal.html';

const reveal = {
  bindings: {},
  template,
  controller,
  style,
};

export default reveal;
